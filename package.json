{
  "name": "star-wars-api",
  "version": "1.0.0",
  "description": "Mock REST API for Star Wars Universe — powered by json-server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node db.js",
    "lint": "node -e \"const db=require('./db.js')(); Object.entries(db).forEach(([k,v])=>console.log(k+': '+v.length+' records'))\""
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Star Wars API — Disney Canon 2014+",
  "license": "MIT"
}
